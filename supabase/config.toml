[functions.healthkit-ingest]
verify_jwt = false
# Auth is handled inside the function: h4_sk_* tokens → healthkit_api_keys lookup,
# Supabase JWTs → auth.getUser(). Gateway JWT check disabled so custom tokens pass through.
